<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.backstageManagement.mapper.EmpMapper">
<!--    更新员工信息-->
    <update id="updateById">
        UPDATE emp
        SET username    = #{username},
            password    = #{ password},
            name        = #{ name},
            gender      = #{ gender},
            phone       = #{ phone},
            job         = #{ job  },
            salary      = #{  salary  },
            image       = #{  image  },
            entry_date  = #{ entryDate },
            dept_id     = #{ deptId },
            update_time = #{ updateTime}
        WHERE id = #{id}
    </update>
    <!--    批量删除员工信息-->
    <delete id="deleteByIds">
        delete from emp where id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>

    <select id="list" resultType="com.backstageManagement.pojo.Emp">
        select e.*,d.name as deptName from emp as e left join dept as d on e.dept_id=d.id
        <where>
            <if test="name!=null and name!=''">
                e.name like concat('%',#{name},'%')
            </if>
            <if test="gender!=null">
                and
                e.gender=#{gender}
            </if>
            <if test="begin!=null and end!=null">
                and
                e.entry_date between #{begin} and #{end}
            </if>
        </where>
        order by entry_date desc
    </select>
<!--    定义resultMap-->
    <resultMap id="empResultMap" type="com.backstageManagement.pojo.Emp">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="name" property="name"/>
        <result column="gender" property="gender"/>
        <result column="phone" property="phone"/>
        <result column="job" property="job"/>
        <result column="salary" property="salary"/>
        <result column="image" property="image"/>
        <result column="entry_date" property="entryDate"/>
        <result column="dept_id" property="deptId"/>
        <result column="create_time" property="entryDate"/>
        <result column="update_time" property="updateTime"/>
<!--        封装员工工作经历信息-->
        <collection property="exprList" ofType="com.backstageManagement.pojo.EmpExpr">
            <id column="ee_id" property="id"/>
            <result column="ee_empid" property="empId"/>
            <result column="ee_begin" property="begin"/>
            <result column="ee_end" property="end"/>
            <result column="ee_company" property="company"/>
            <result column="ee_job" property="job"/>
        </collection>
    </resultMap>
<!--    根据ID查询员工信息以及工作经历-->
    <select id="getById" resultMap="empResultMap">
        select e.*,
               ee.id as   ee_id,
               ee.emp_id  ee_empid,
               ee.begin   ee_begin,
               ee.end     ee_end,
               ee.company ee_company,
               ee.job     ee_job
        from emp as e
                 left join emp_expr as ee on e.id = ee.emp_id
        where e.id = #{id}
    </select>
<!--    统计员工职位人数-->
    <select id="countEmpJobData" resultType="java.util.Map">
        select (case
                    when job = 1 then '班主任'
                    when job = 2 then '讲师'
                    when job = 3 then '学工主管'
                    when job = 4 then '教研主管'
                    when job = 5 then '咨询师'
                    else '其他职位' end) as position,
               count(*)                  as num
        from emp
        group by job
        order by num;
    </select>
    <select id="countEmpGenderData" resultType="java.util.Map">
        select if(emp.gender = 1, '男性员工', '女性员工') as name,
               count(*)                                   as value
        from emp
        group by gender;
    </select>
<!--    查询全部员工信息-->
    <select id="getAll" resultType="com.backstageManagement.pojo.Emp">
        select emp.id,
               emp.username,
               emp.password,
               emp.name,
               emp.gender,
               emp.image,
               emp.job,
               emp.salary,
               emp.entry_date,
               emp.dept_id,
               emp.create_time,
               emp.update_time
        from emp;
    </select>
<!--    查询部门员工数量-->
    <select id="countByDeptId" resultType="java.lang.Integer">
        select count(id)
        from emp
        where dept_id = #{deptId}
    </select>
</mapper>