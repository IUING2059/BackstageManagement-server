<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.backstageManagement.mapper.ClazzsMapper">
    <!--    添加新班级-->
    <insert id="insert">
        insert into clazz(name, room, begin_date, end_date, master_id, subject, create_time, update_time)
        VALUES (#{name}, #{room}, #{beginDate}, #{endDate}, #{masterId}, #{subject}, #{createTime}, #{updateTime})
    </insert>
    <update id="updateById">
        update clazz
        <set>
            <if test="name != null and name!=''">name=#{name},</if>
            <if test="room != null">room=#{room},</if>
            <if test="beginDate != null">begin_date=#{beginDate},</if>
            <if test="endDate != null">end_date=#{endDate},</if>
            <if test="masterId != null">master_id=#{masterId},</if>
            <if test="subject != null">subject=#{subject},</if>
            update_time=#{updateTime}
        </set>
        where id=#{id}
    </update>

    <!--    查找全部班级-->
    <select id="findAll" resultType="com.backstageManagement.pojo.Clazz">
        select c.id,
        c.name,
        c.room,
        c.begin_date,
        c.end_date,
        c.master_id,
        c.subject,
        c.create_time,
        c.update_time,
        e.name as masterName
        from clazz as c
        left join emp as e on c.master_id = e.id
        <where>
            <if test="name != null and name != ''">
                c.name like concat('%',#{name},'%')
            </if>
            <if test="begin != null and begin != ''">
                c.end_date >= #{begin}
            </if>
            <if test="end != null and end != ''">
                c.end_date&lt;= #{end}
            </if>
        </where>
        order by c.update_time desc
        limit #{start}, #{pageSize}
    </select>

    <!--    统计总数-->
    <select id="countTotal" resultType="java.lang.Long">
        select count(*)
        from clazz c
        <where>
            <if test="name != null and name != ''">
                c.name like concat('%',#{name},'%')
            </if>
            <if test="begin != null and begin != ''">
                c.end_date >= #{begin}
            </if>
            <if test="end != null and end != ''">
                c.end_date &lt;= #{end}
            </if>
        </where>
    </select>



    <!--    根据ID查找班级-->
    <select id="getById" resultType="com.backstageManagement.pojo.Clazz">
        select c.*, e.name as masterName
        from clazz as c
                 left join emp as e on c.master_id = e.id
        where c.id = #{id};
    </select>
    <select id="findToAll" resultType="com.backstageManagement.pojo.Clazz">
        select id,
               name,
               room,
               begin_date,
               end_date,
               master_id,
               subject,
               create_time,
               update_time
        from clazz;
    </select>
</mapper>